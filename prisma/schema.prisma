generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Pun {
  id       Int @id @unique @default(autoincrement())
  question String
  answer   String
}
